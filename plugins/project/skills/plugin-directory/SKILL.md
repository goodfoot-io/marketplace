---
name: plugin-directory
description: Adds the plugin directory to the context.
---

The **Plugin Directory** is the grandparent of the **Base Directory**.

For example if the base directory of this skill was `plugins/example/skills/plugin-directory` the plugin directory would be `plugins/example`.
