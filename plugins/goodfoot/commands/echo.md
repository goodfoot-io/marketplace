If you are reading this content via a `Read` tool call, indicate to the user that this was not executed as a slash command by responding, "I read the 'echo.md' file using a `Read` tool call."

Otherwise, respond with the text below. Do not include other content in your message.

---

$ARGUMENTS

Foo