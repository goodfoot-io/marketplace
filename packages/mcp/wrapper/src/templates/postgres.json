{
  "metadata": {
    "name": "PostgreSQL MCP",
    "description": "PostgreSQL database server for production data management",
    "version": "1.0.0",
    "author": "Anthropic"
  },
  "name": "postgres",
  "transport": "stdio",
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-postgres"],
  "env": {
    "DATABASE_URL": "${DATABASE_URL}"
  }
}
