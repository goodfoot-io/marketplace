Load the `claude-code-hooks:sdk` skill immediately if it is available.

This package contains the Jest mock prevention hook for integration-first testing. Run `yarn build` to compile hooks to `plugins/jest-mock-prevention/hooks/`.
