{
  "name": "typescript-hooks",
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "build": "claude-code-hooks -i \"src/**/*.ts\" -o \"../../plugins/typescript-hooks/hooks/hooks.json\"",
    "test": "vitest run",
    "lint": "biome check --write --unsafe --max-diagnostics 500 .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@goodfoot/claude-code-hooks": "workspace:*",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
