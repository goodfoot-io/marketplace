packages/typescript-metrics/src/lib/MetricsRunner.ts rk64:75b473339e69894d
packages/voice/src/index.ts rk64:97fc8438199c6653
plugins-claude/voice/bin/voice-mcp-server.mjs rk64:a703df4907042cde
skills/typescript-metrics/bin/typescript-metrics.mjs rk64:04a3f09704e14521

Each committed plugin bundle is compiled from its package sources by the repo build (the metrics CLI into the repo-root skills/typescript-metrics/bin tree, the Claude-only voice server into its plugin's own bin/), and a source change without regenerating and committing the bundles leaves the shipped plugins stale; no test or CI step compares committed bundles against sources, so these two pairs stand for the whole tree of generated bundle artifacts.
