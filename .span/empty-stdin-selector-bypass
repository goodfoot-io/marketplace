packages/jsdoczoom/CHANGELOG.md#L9-L10 rk64:5feae741b260726a
packages/jsdoczoom/package.json#L3-L3 rk64:e4aab900f8543446
packages/jsdoczoom/src/cli.ts#L552-L552 rk64:b159447447f85464

The empty-stdin guard (stdin !== undefined && stdin.trim().length > 0) is the behavioral fix that caused the 1.2.5 release; the version and changelog entry document exactly this change, so modifying the guard or its semantics requires updating the release record, and updating the release record implies the guard was changed.
