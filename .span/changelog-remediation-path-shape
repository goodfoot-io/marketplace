scripts/changelog-surfaces.mjs#L42-L50 rk64:2143f79897e2f814
scripts/check-changelog-entry.mjs#L80-L104 rk64:2f11da0c5eef172b

changelogSurfaces() treats <claudePluginRoot>/CHANGELOG.md as the plugin-level release-notes path, and remediation() independently matches that same shape to decide update-package-changelog.sh cannot write it; if the plugin-root convention ever changes in one function the other silently gives wrong advice, since no test asserts the two path shapes agree.
