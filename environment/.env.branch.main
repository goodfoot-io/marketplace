# Branch-specific environment template
# Copy this file to create your branch-specific environment:
#   cp environment/.env.branch.template environment/.env.branch.main
#   cp environment/.env.branch.template environment/.env.branch.feature-name
#
# This file should contain NON-SECRET configuration that varies by branch
# For secrets or machine-specific config, use .env.local (gitignored)

# Environment identifier (auto-set by direnv based on branch name)
# ENVIRONMENT=<branch-name>

# Example: Branch-specific API endpoints
# API_ENDPOINT=http://localhost:3000
# DATABASE_NAME=mydb_dev

# Example: Feature flags per branch
# ENABLE_NEW_FEATURE=false
# DEBUG_MODE=true

# Example: Branch-specific resource identifiers
# AWS_STACK_NAME=myapp-dev
# DEPLOYMENT_ENV=development

# Add your branch-specific (non-secret) configuration below:
ENVIRONMENT=main
